import React from 'react';

const SizeSelector = () => (
  <select aria-label="Select Passport Size Preset">
    <option value="us">US Visa (2x2in)</option>
  </select>
);

export default SizeSelector;